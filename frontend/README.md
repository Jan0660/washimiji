# frontend

```sh
# install packages
pnpm i
# run for developing
pnpm dev
# buld site
pnpm build
```

## Config

- `VITE_API_URL` - Accessible URL of the API
- `VITE_STATIC_FILES` - URL where font files are accessible
